version: '3'
services:
    hotel:
      build:
        context: build/app
      image: berwoutv/hotel:berwout

    hotel-envoy:
      build:
        context:
          build/hotel-envoy
      image: berwoutv/hotel-envoy:berwout

    front-envoy:
      build:
        context:
          build/front-envoy
      image: berwoutv/front-envoy:berwout
